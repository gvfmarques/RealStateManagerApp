function ApproveUser("/User/ApproveUser"; $.ajax({ method: 'POST', url: url, data: {== !0){
    $('#' + "btn-floating blue darken-4"href = "User/UserManagement?' + ' + name + '"asp - controller="User"asp - action="UserManagement"asp - route - "' + "' + name + '"><i class="material - icons">group</i></a>');

                M.toast({
        html: "User approved",
        classes: "green darken-3"
    });
}
            else {
    M.toast({
        html: "Unable to approve user"
    });
}
        }
    })
}

function DisapproveUser( "/User/DisapproveUser";

    $.ajax({
        method: 'POST',
        url: url,
        data: { == true) {
    $("#" + userId).removeClass("purple darken-3").addClass("orange darken-3").text("Disapproved");
    $("." + userId).remove();

    M.toast({
        html: "Disapproved User",
        classes: "orange darken-3"
    });
}

            else {
    M.toast({
        html: "Unable to approve user"
    })
}}})}